{
  "name": "share-and-save",
  "version": "1.0.0",
  "description": "Web application to manage and analyze household accounts",
  "main": "index.js",
  "scripts": {
    "start": "cd server/src && tsc && cd ../dist && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "typescript": "^5.0.2"
  },
  "engines": {
    "node": "19.8.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.10"
  }
}
