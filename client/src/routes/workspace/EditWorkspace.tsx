import React from "react";
import Workspace from "../../objects/entities/Workspace";

interface EditWorkspaceProps {
    workspace: Workspace
}

const EditWorkspace = (props: EditWorkspaceProps): JSX.Element => {

    return (<>
        Edit workspace
    </>);
}

export default EditWorkspace;
